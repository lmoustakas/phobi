# input file for phobi

FILE_LIST   = ../data/FITS_file_list.txt	# (string) text file listing directory path and file name of images to be analyzed.
TAG	    = LCOGT_HE0435  			# (string) tag to be used for output file names
DATE_APPEND = True				# (TRUE/FALSE) append the date and time to the output file names. This prevents overwriting and loss of data.
PREPROCESS  = True				# Preprocess the data 
PHOTOMETER  = False				# Perform photmetric estimation on the data
INFERDELAY  = False				# Infer delays

